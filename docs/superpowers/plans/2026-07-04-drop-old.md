# `gzh drop-old` 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 新增 `gzh drop-old` 子命令——按「保留最新 N 个非 liveup」规则清理旧 ebuild，用 portage `vercmp` 正确排序，默认 dry-run、`--apply` 才删 + 重算 Manifest、不自动 commit。

**Architecture:** 业务逻辑放 `gzh/gzh/drop_old.py` 纯函数（`list_ebuilds`/`drop_candidates` 用 vercmp 排序、9999 排除、keep 计算；`run_drop_old` 编排 dry-run/apply + manifest），click 薄壳在 `cli.py`。复用 MVP 的 `run_manifest`、`find_overlay_root`；无新依赖（portage 系统自带）。

**Tech Stack:** Python 3.11+、click、`portage.versions.vercmp`（系统库）、`functools.cmp_to_key`；pytest（真实 vercmp + tmp_path fixture，manifest mock runner）。

## Global Constraints

- **安全第一**：默认 dry-run（不删）；`--apply` 才删 + Manifest；**不自动 commit**（用户 review 后手动 `gzh commit`）。
- **9999 liveup 保护**：`PV == "9999"` 或 `startswith("9999")` 一律保留，不计入 keep、不被 drop。
- **vercmp 排序**：用 `portage.versions.vercmp`（非字典序），`1.10` > `1.9`。
- **去个人化**：overlay 根走 `find_overlay_root()`；无个人路径硬编码。
- **commit 无 AI 署名**（`Co-Authored-By`/`🤖 Generated` 一律不写）。
- **TDD**：每步先写失败测试 → 确认失败 → 实现 → 通过 → commit。
- **不碰** `/var/db/repos/gentoo-zh`（synced 副本）。

## File Structure

```
gzh/
├── gzh/
│   ├── drop_old.py    # 新增：list_ebuilds / drop_candidates / run_drop_old / _enumerate_pkgs
│   └── cli.py         # 修改：注册 drop-old 命令
└── tests/
    └── test_drop_old.py  # 新增：L1（真实 vercmp + tmp_path + mock manifest runner）
```

---

### Task 1: `list_ebuilds` + `drop_candidates` 纯函数

**Files:**
- Create: `gzh/gzh/drop_old.py`
- Test: `gzh/tests/test_drop_old.py`

**Interfaces:**
- Produces:
  - `list_ebuilds(pkg_dir: Path, pn: str) -> list[Path]`（glob `<pn>-*.ebuild`，未排序）
  - `drop_candidates(ebuilds: list[Path], pn: str, keep: int = 2, vercmp=portage.versions.vercmp) -> tuple[list[Path], list[Path]]`（返回 `(dropped, kept)`；排除 9999，其余按 vercmp 降序，保留前 keep 个，余为 dropped；kept 含保留的 + liveup）

- [ ] **Step 1: 写失败测试**

`gzh/tests/test_drop_old.py`:
```python
from pathlib import Path

from gzh.drop_old import drop_candidates, list_ebuilds


def _pkgdir(tmp_path, versions, pn="foo"):
    d = tmp_path / "cat" / pn
    d.mkdir(parents=True)
    for v in versions:
        (d / f"{pn}-{v}.ebuild").write_text("EAPI=8\n")
    return d


def test_list_ebuilds_globs_by_pn(tmp_path):
    d = _pkgdir(tmp_path, ["1.0", "2.0"])
    ebs = list_ebuilds(d, "foo")
    assert sorted(p.name for p in ebs) == ["foo-1.0.ebuild", "foo-2.0.ebuild"]


def test_drop_keeps_newest_n(tmp_path):
    d = _pkgdir(tmp_path, ["1.0", "1.1", "1.2", "1.3"])
    dropped, kept = drop_candidates(list_ebuilds(d, "foo"), "foo", keep=2)
    assert [p.name for p in dropped] == ["foo-1.0.ebuild", "foo-1.1.ebuild"]
    assert [p.name for p in kept] == ["foo-1.2.ebuild", "foo-1.3.ebuild"]


def test_drop_vercmp_not_lexicographic(tmp_path):
    # vercmp: 1.10 > 1.9 ; lexicographic would wrongly put 1.9 newer
    d = _pkgdir(tmp_path, ["1.9", "1.10"])
    dropped, kept = drop_candidates(list_ebuilds(d, "foo"), "foo", keep=1)
    assert [p.name for p in dropped] == ["foo-1.9.ebuild"]
    assert [p.name for p in kept] == ["foo-1.10.ebuild"]


def test_drop_keeps_9999_liveup_always(tmp_path):
    d = _pkgdir(tmp_path, ["1.0", "2.0", "9999"])
    dropped, kept = drop_candidates(list_ebuilds(d, "foo"), "foo", keep=1)
    assert [p.name for p in dropped] == ["foo-1.0.ebuild"]
    assert sorted(p.name for p in kept) == ["foo-2.0.ebuild", "foo-9999.ebuild"]


def test_drop_revision_sort(tmp_path):
    d = _pkgdir(tmp_path, ["1.0", "1.0-r1", "1.0-r2"])
    dropped, kept = drop_candidates(list_ebuilds(d, "foo"), "foo", keep=1)
    assert [p.name for p in dropped] == ["foo-1.0.ebuild", "foo-1.0-r1.ebuild"]
    assert [p.name for p in kept] == ["foo-1.0-r2.ebuild"]


def test_drop_nothing_when_within_keep(tmp_path):
    d = _pkgdir(tmp_path, ["1.0", "2.0"])
    dropped, kept = drop_candidates(list_ebuilds(d, "foo"), "foo", keep=2)
    assert dropped == []
    assert sorted(p.name for p in kept) == ["foo-1.0.ebuild", "foo-2.0.ebuild"]
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd gzh && python -m pytest tests/test_drop_old.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'gzh.drop_old'`

- [ ] **Step 3: 实现**

`gzh/gzh/drop_old.py`:
```python
from __future__ import annotations

from functools import cmp_to_key
from pathlib import Path

from portage.versions import vercmp


def list_ebuilds(pkg_dir: Path, pn: str) -> list[Path]:
    return list(Path(pkg_dir).glob(f"{pn}-*.ebuild"))


def _pv_from_name(name: str, pn: str) -> str:
    stem = name.removesuffix(".ebuild")
    return stem[len(pn) + 1:]


def _is_liveup(pv: str) -> bool:
    return pv == "9999" or pv.startswith("9999")


def drop_candidates(ebuilds: list[Path], pn: str, keep: int = 2,
                    vercmp=vercmp) -> tuple[list[Path], list[Path]]:
    liveup: list[Path] = []
    rest: list[tuple[str, Path]] = []
    for eb in ebuilds:
        pv = _pv_from_name(eb.name, pn)
        if _is_liveup(pv):
            liveup.append(eb)
        else:
            rest.append((pv, eb))
    # descending by version (newest first): cmp(a,b) = vercmp(b_pv, a_pv)
    rest.sort(key=cmp_to_key(lambda a, b: vercmp(b[0], a[0])))
    rest_ebs = [eb for _, eb in rest]
    dropped = rest_ebs[keep:]
    kept = rest_ebs[:keep] + liveup
    return dropped, kept
```

- [ ] **Step 4: 跑测试确认通过**

Run: `cd gzh && python -m pytest tests/test_drop_old.py -v`
Expected: PASS（6 passed）

- [ ] **Step 5: Commit**

```bash
git add gzh/gzh/drop_old.py gzh/tests/test_drop_old.py
git commit -m "feat(gzh): add drop_candidates with vercmp sort and liveup protection"
```

---

### Task 2: `run_drop_old` 编排 + `_enumerate_pkgs`

**Files:**
- Modify: `gzh/gzh/drop_old.py`（追加 `_enumerate_pkgs`、`run_drop_old`）
- Test: `gzh/tests/test_drop_old.py`（追加测试）

**Interfaces:**
- Consumes: `list_ebuilds`, `drop_candidates`（Task 1）；`run_manifest`（MVP）；`find_overlay_root`
- Produces:
  - `_enumerate_pkgs(root: Path, target: str) -> list[str]`（`target="all"` 遍历 `root/*/*/` 含 ebuild 的包目录；否则视为单 `cat/pkg` 返回 `[target]`）
  - `run_drop_old(target: str, keep: int = 2, apply: bool = False, overlay_root: Path | None = None, manifest_runner=subprocess.run) -> dict`（返回 `{"ok": True, "results": [{"cat_pkg", "dropped": [name], "kept": [name], "manifest_ok"?}]}`；dry-run 不删；apply 删 dropped + 每包 `run_manifest`）

- [ ] **Step 1: 写失败测试**

追加到 `gzh/tests/test_drop_old.py`:
```python
import subprocess

from gzh.drop_old import _enumerate_pkgs, run_drop_old


def _overlay(tmp_path):
    # 两个包：foo 多版本（会 drop），bar 单版本（不 drop）
    foo = tmp_path / "cat" / "foo"
    foo.mkdir(parents=True)
    for v in ["1.0", "1.1", "1.2"]:
        (foo / f"foo-{v}.ebuild").write_text("EAPI=8\n")
    bar = tmp_path / "cat" / "bar"
    bar.mkdir(parents=True)
    (bar / "bar-1.0.ebuild").write_text("EAPI=8\n")
    notpkg = tmp_path / "cat" / "empty"
    notpkg.mkdir(parents=True)
    return tmp_path


def test_enumerate_pkgs_all(tmp_path):
    root = _overlay(tmp_path)
    assert _enumerate_pkgs(root, "all") == ["cat/bar", "cat/foo"]


def test_enumerate_pkgs_single(tmp_path):
    assert _enumerate_pkgs(tmp_path, "cat/foo") == ["cat/foo"]


def test_run_drop_old_dry_run_does_not_delete(tmp_path):
    root = _overlay(tmp_path)
    res = run_drop_old("all", keep=2, apply=False, overlay_root=root)
    assert res["ok"] is True
    by_pkg = {r["cat_pkg"]: r for r in res["results"]}
    assert by_pkg["cat/foo"]["dropped"] == ["foo-1.0.ebuild"]
    assert by_pkg["cat/bar"]["dropped"] == []
    # dry-run: files still there
    assert (root / "cat" / "foo" / "foo-1.0.ebuild").exists()


def test_run_drop_old_apply_deletes_and_manifests(tmp_path):
    root = _overlay(tmp_path)
    manifest_calls = []

    def fake_manifest_runner(args, **kw):
        manifest_calls.append(args)
        return subprocess.CompletedProcess(args, 0, "", "")

    res = run_drop_old("all", keep=2, apply=True, overlay_root=root,
                       manifest_runner=fake_manifest_runner)
    by_pkg = {r["cat_pkg"]: r for r in res["results"]}
    # foo had a drop -> file deleted + manifest called
    assert not (root / "cat" / "foo" / "foo-1.0.ebuild").exists()
    assert by_pkg["cat/foo"]["manifest_ok"] is True
    # bar had no drop -> no manifest call for it
    assert "manifest_ok" not in by_pkg["cat/bar"]
    # manifest called once (for foo only)
    assert len(manifest_calls) == 1


def test_run_drop_old_manifest_failure_recorded(tmp_path):
    root = _overlay(tmp_path)

    def fake_manifest_runner(args, **kw):
        return subprocess.CompletedProcess(args, 1, "", "fetch fail")

    res = run_drop_old("all", keep=2, apply=True, overlay_root=root,
                       manifest_runner=fake_manifest_runner)
    by_pkg = {r["cat_pkg"]: r for r in res["results"]}
    assert by_pkg["cat/foo"]["manifest_ok"] is False
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd gzh && python -m pytest tests/test_drop_old.py -v`
Expected: FAIL — `ImportError: cannot import name '_enumerate_pkgs'`

- [ ] **Step 3: 实现**

在 `gzh/gzh/drop_old.py` 顶部 import 区补：
```python
import subprocess
from gzh.manifest import run_manifest
from gzh.repo import find_overlay_root
```
追加：
```python
def _enumerate_pkgs(root: Path, target: str) -> list[str]:
    root = Path(root)
    if target != "all":
        return [target]
    out: list[str] = []
    for cat_dir in root.iterdir():
        if not cat_dir.is_dir() or cat_dir.name.startswith("."):
            continue
        for pkg_d in cat_dir.iterdir():
            if pkg_d.is_dir() and any(pkg_d.glob("*.ebuild")):
                out.append(f"{cat_dir.name}/{pkg_d.name}")
    return sorted(out)


def run_drop_old(target: str, keep: int = 2, apply: bool = False,
                 overlay_root: Path | None = None,
                 manifest_runner=subprocess.run) -> dict:
    root = Path(overlay_root) if overlay_root else find_overlay_root()
    results = []
    for cat_pkg in _enumerate_pkgs(root, target):
        cat, pn = cat_pkg.split("/", 1)
        pkg_dir = root / cat / pn
        if not pkg_dir.is_dir():
            continue
        ebs = list_ebuilds(pkg_dir, pn)
        dropped, kept = drop_candidates(ebs, pn, keep=keep)
        entry = {"cat_pkg": cat_pkg,
                 "dropped": [p.name for p in dropped],
                 "kept": [p.name for p in kept]}
        if apply and dropped:
            for p in dropped:
                (pkg_dir / p.name).unlink()
            if kept:
                mres = run_manifest(pkg_dir / kept[0].name, cwd=root,
                                    runner=manifest_runner)
                entry["manifest_ok"] = mres["ok"]
            else:
                entry["manifest_ok"] = True
        results.append(entry)
    return {"ok": True, "results": results}
```

- [ ] **Step 4: 跑测试确认通过 + 全量回归**

Run: `cd gzh && python -m pytest tests/test_drop_old.py -v && python -m pytest -q`
Expected: test_drop_old.py 全 PASS（11 passed）；全量无回归。

- [ ] **Step 5: Commit**

```bash
git add gzh/gzh/drop_old.py gzh/tests/test_drop_old.py
git commit -m "feat(gzh): run_drop_old orchestrates dry-run/apply with manifest recompute"
```

---

### Task 3: cli 注册 `drop-old`

**Files:**
- Modify: `gzh/gzh/cli.py`（注册 `drop-old`）
- Test: `gzh/tests/test_drop_old.py`（追加 cli 测试）

**Interfaces:**
- Consumes: `run_drop_old`（Task 2）
- Produces: `gzh drop-old [--all | --pkg <cat/pkg>] [--keep N=2] [--apply]`。`--all`/`--pkg` 互斥（都传或都不传 → click 报错）。

- [ ] **Step 1: 写失败测试**

追加到 `gzh/tests/test_drop_old.py`:
```python
from click.testing import CliRunner

from gzh.cli import cli


def test_drop_old_help_registered():
    result = CliRunner().invoke(cli, ["drop-old", "--help"])
    assert result.exit_code == 0
    assert "keep" in result.output.lower()


def test_drop_old_requires_all_or_pkg():
    result = CliRunner().invoke(cli, ["drop-old"])
    assert result.exit_code != 0  # mutual exclusion / required


def test_drop_old_all_and_pkg_mutually_exclusive():
    result = CliRunner().invoke(cli, ["drop-old", "--all", "--pkg", "a/b"])
    assert result.exit_code != 0


def test_drop_old_dry_run_via_cli(tmp_path, monkeypatch):
    import gzh.cli as cli_mod
    monkeypatch.setattr("gzh.cli.find_overlay_root", lambda: tmp_path)
    foo = tmp_path / "cat" / "foo"
    foo.mkdir(parents=True)
    for v in ["1.0", "1.1", "1.2"]:
        (foo / f"foo-{v}.ebuild").write_text("EAPI=8\n")
    result = CliRunner().invoke(cli_mod.cli,
                                ["drop-old", "--pkg", "cat/foo", "--keep", "2"])
    assert result.exit_code == 0
    assert "foo-1.0.ebuild" in result.output  # listed as dropped
    assert (foo / "foo-1.0.ebuild").exists()  # dry-run: not deleted
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd gzh && python -m pytest tests/test_drop_old.py::test_drop_old_help_registered -v`
Expected: FAIL — `Error: No such command 'drop-old'`

- [ ] **Step 3: 实现（注册命令）**

在 `gzh/gzh/cli.py`：
- 顶部 import 区追加（与其他 `from gzh.<mod> import ...` 同级）：
```python
from gzh.drop_old import run_drop_old
```
- 在 `bump_issues_cmd` 之后、`def main():` 之前追加（位置紧随 triage/notify 之后亦可，保持命令聚集）：
```python
@cli.command("drop-old")
@click.option("--all", "all_", is_flag=True, default=False, help="scan all packages")
@click.option("--pkg", default=None, help="single cat/pkg")
@click.option("--keep", default=2, show_default=True, type=int)
@click.option("--apply", is_flag=True, default=False,
              help="actually delete + recompute Manifest (default: dry-run)")
def drop_old_cmd(all_, pkg, keep, apply):
    """List or drop old ebuild versions (keep newest N non-liveup; *-9999 kept)."""
    if all_ and pkg:
        raise click.UsageError("--all and --pkg are mutually exclusive")
    if not all_ and not pkg:
        raise click.UsageError("specify --all or --pkg")
    target = "all" if all_ else pkg
    res = run_drop_old(target, keep=keep, apply=apply)
    click.echo(_json.dumps(res, indent=2, ensure_ascii=False))
```

- [ ] **Step 4: 跑测试确认通过 + 全量回归**

Run: `cd gzh && python -m pytest tests/test_drop_old.py -v && python -m pytest -q`
Expected: test_drop_old.py 全 PASS（15 passed）；全量无回归。

- [ ] **Step 5: 手动验证**

Run: `gzh drop-old --help` → 应见 `--all/--pkg/--keep/--apply`。
Run: `gzh drop-old --all --pkg x` → 应报互斥错误（非 0 退出）。

- [ ] **Step 6: Commit**

```bash
git add gzh/gzh/cli.py gzh/tests/test_drop_old.py
git commit -m "feat(gzh): register drop-old command (dry-run default, --all/--pkg mutex)"
```

---

### Task 4: L3 真实端到端验证

**Files:** 无（运行验证）

**Interfaces:** 对真实 gentoo-zh 副本跑 dry-run + apply（单包，安全）。

- [ ] **Step 1: dry-run 单包（cc-switch-cli 多版本）**

Run（在 skills 仓库根，`GZH_OVERLAY_DIR` 指向 skills 内 gentoo-zh 副本）:
```bash
export GZH_OVERLAY_DIR=/home/yongxiang/work/gentoo/gentoo-zh-skills/gentoo-zh
gzh drop-old --pkg app-misc/cc-switch-cli --keep 2
```
Expected: cc-switch-cli 有 5.4.0/5.5.0/5.8.7（若 5.8.7 还在；若之前清理则 5.4.0/5.5.0）→ 列出 drop 最老 1 个，保留最新 2；**文件不删**（dry-run）。

- [ ] **Step 2: dry-run --all（全量扫描）**

Run: `gzh drop-old --all --keep 2 | python3 -c 'import sys,json;d=json.load(sys.stdin);drop_pkgs=[r for r in d["results"] if r["dropped"]];print("pkgs with drops:",len(drop_pkgs));[print(" ",r["cat_pkg"],r["dropped"]) for r in drop_pkgs[:5]]'`
Expected: 多包有 drop 候选（3+ 版本的包），文件不删。

- [ ] **Step 3: apply 单包（在测试副本，确认删 + Manifest）**

选一个有 3+ 版本的包（如 `app-misc/cc-switch-cli`），在 skills 内副本跑：
```bash
gzh drop-old --pkg app-misc/cc-switch-cli --keep 2 --apply
```
Expected: 删除最老 ebuild + pkgdev manifest 重算（真实 pkgdev）；`manifest_ok=True`；文件确认删除。

- [ ] **Step 4: 若 L3 发现缺陷**

修复并补单测（同前：缺陷 → 失败测试 → 修复 → 全量绿 → commit）。验证后**恢复副本**（git checkout 重置删掉的 ebuild，或在 throwaway 分支跑 apply）。

---

## Self-Review

**1. Spec coverage（spec §8 验收）：**
- 验收#1（dry-run 单包列 drop，不删）：Task 4 Step 1 + Task 2 `test_run_drop_old_dry_run_does_not_delete` ✅
- 验收#2（--apply 删 + Manifest）：Task 2 `test_run_drop_old_apply_deletes_and_manifests` + Task 4 Step 3 ✅
- 验收#3（--all dry-run 多包）：Task 2 `_enumerate_pkgs`/`run_drop_old` + Task 4 Step 2 ✅
- 验收#4（9999 keep 之外保留）：Task 1 `test_drop_keeps_9999_liveup_always` ✅
- 验收#5（vercmp 1.10>1.9）：Task 1 `test_drop_vercmp_not_lexicographic` ✅
- 验收#6（L1 全绿、dry-run 不删）：Task 1-3 全 mock + Task 2 dry-run 测试 ✅

**2. Placeholder scan：** 无 TBD/TODO；每步含完整测试代码、实现代码、命令、预期。✅

**3. Type consistency：** `list_ebuilds(pkg_dir, pn) -> list[Path]`、`drop_candidates(ebuilds, pn, keep=2, vercmp=) -> tuple[list[Path], list[Path]]`、`_enumerate_pkgs(root, target) -> list[str]`、`run_drop_old(target, keep=2, apply=False, overlay_root=None, manifest_runner=) -> dict` 在 Task 1-3 间一致；返回 `{cat_pkg, dropped:[name], kept:[name], manifest_ok?}` 统一；9999 判定（`pv=="9999" or startswith("9999")`）在 `_is_liveup` 单点定义；vercmp 降序（`cmp_to_key(lambda a,b: vercmp(b[0], a[0]))`）在 drop_candidates 单点。✅
